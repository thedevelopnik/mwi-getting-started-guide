---
version: v2
proxy_server: mwidemo.cloud.gravitational.io:443 # replace with your Teleport cluster address
onboarding:
  join_method: github
  token: github-bot
certificate_ttl: 5m
storage:
  type: memory
outputs:
  - type: identity
    destination:
      type: directory
      path: ./ssh_out